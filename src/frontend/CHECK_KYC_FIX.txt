URGENT FIX NEEDED - Line 895 in RestaurantSetupWizard.jsx

Current (WRONG):
```
				<Step2
					formData={formData}
					handleChange={handleChange}
					kycStatus={kycStatus}
					onVerifyIdentity={handleVerifyIdentity}
					kycLoading={kycLoading}
				/>
```

Should be (CORRECT):
```
				<Step2
					formData={formData}
					handleChange={handleChange}
					kycStatus={kycStatus}
					onVerifyIdentity={handleVerifyIdentity}
					checkKYCStatus={checkKYCStatus}  ← ADD THIS LINE
					kycLoading={kycLoading}
				/>
```

The button "Check Verification Status" is calling checkKYCStatus but it's not being passed as a prop!
